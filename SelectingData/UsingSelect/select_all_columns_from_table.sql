USE SlickShop;
--Selecting all the Columns from a Table.

--Manually Asking for each individual column.
SELECT PartID, Description, Cost
    FROM Part;

-- Using the * Expression to Ask for all of them
SELECT *
    FROM Part;